import React from 'react'

export default function Person({personist}) {
  return (
    <div>
        <h2>{personist.name }</h2>

      
    </div>
    //console throughs an error that keys should be used 
  )
}
